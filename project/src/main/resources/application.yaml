spring:
  application:
    name: project

  security:
    user:
      name: admin
      password: 123

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/da1}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:1234567}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  liquibase:
    enabled: false
  
  jwt:
    secret: ${JWT_SECRET:mySecretKeyForJwtAuthenticationProject2026VeryLongKey}
    expiration: ${JWT_EXPIRATION:86400000}
